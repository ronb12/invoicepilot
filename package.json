{
  "name": "invoicepilot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check api/bootstrap.js && node --check api/clients.js && node --check api/invoices.js && node --check api/followups.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
